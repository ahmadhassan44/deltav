{
  "name": "deltav-landing",
  "scripts": {
    "build": "node scripts/build.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0",
    "wrangler": "^4.0.0"
  }
}
